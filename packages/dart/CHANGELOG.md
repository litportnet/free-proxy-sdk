# Changelog

## 0.1.0

- Initial release.
- `LitportFreeProxyClient` with `getProxies` and `pickBest`.
- `ProxyFilters`, `FreeProxy`, and typed exceptions for snapshot and filter
  validation.
- Zero runtime dependencies (`dart:io` and `dart:convert` only).
